\begin{figure}[H]
    \centering
    \resizebox{0.88\textwidth}{!}{%
    \begin{tikzpicture}[
        font=\normalfont\small,
        >=Stealth,
        input/.style={
            rounded rectangle,
            draw=black!55,
            fill=black!4,
            line width=0.7pt,
            minimum width=2.45cm,
            minimum height=0.78cm,
            align=center
        },
        block/.style={
            rounded rectangle,
            draw=TUgreen!65!black,
            fill=TUgreen!8,
            line width=0.9pt,
            minimum width=3.05cm,
            minimum height=0.86cm,
            align=center
        },
        output/.style={
            rounded rectangle,
            draw=orange!80!black,
            fill=orange!10,
            line width=0.9pt,
            minimum width=2.85cm,
            minimum height=0.86cm,
            align=center
        },
        arrow/.style={->, line width=0.85pt, draw=black!65},
        faint/.style={->, line width=0.65pt, draw=red!70!black}
    ]

    \node[input] (tokens) at (0,0) {Tokens\\+ position};
    \node[block] (qkv) at (3.35,0) {Query, key, value\\projections};
    \node[block] (attn) at (6.75,0) {Multi-head\\self-attention};
    \node[block] (ffn) at (10.0,0) {Feed-forward\\update};
    \node[output] (next) at (13.25,0) {Next-token\\probabilities};

    \draw[arrow] (tokens) -- (qkv);
    \draw[arrow] (qkv) -- (attn);
    \draw[arrow] (attn) -- (ffn);
    \draw[arrow] (ffn) -- (next);

    \node[font=\normalfont\scriptsize, align=center] (t1) at (5.85,1.25) {token};
    \node[font=\normalfont\scriptsize, align=center] (t2) at (6.75,1.25) {token};
    \node[font=\normalfont\scriptsize, align=center] (t3) at (7.65,1.25) {token};
    \draw[faint, bend left=22] (t1) to (t2);
    \draw[faint, bend left=22] (t2) to (t3);
    \draw[faint, bend left=28] (t3) to (t1);

    \node[font=\normalfont\scriptsize, align=center, text=black!70] at (6.75,-0.92)
        {context-dependent token representations};

    \end{tikzpicture}%
    }
    \caption{Simplified view of a generative transformer block. Tokens with positional information are projected into query, key, and value vectors, contextualised through multi-head self-attention, refined by a feed-forward layer, and mapped to probabilities for the next token \cite{vaswani2017attention}.}
    \label{fig:transformer-attention-placeholder}
\end{figure}
