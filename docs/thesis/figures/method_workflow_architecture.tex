% Figure 4.2: Conceptual SIMONE workflow architecture.
\begin{tikzpicture}[
    font=\small,
    >=Stealth,
    wfstep/.style={
        draw=TUgreen!80!black,
        fill=TUgreen!8,
        very thick,
        rounded corners=3pt,
        minimum width=2.85cm,
        minimum height=1.05cm,
        align=center
    },
    data/.style={
        draw=black!55,
        fill=black!4,
        thick,
        rounded corners=3pt,
        minimum width=2.85cm,
        minimum height=1.05cm,
        align=center
    },
    decision/.style={
        draw=orange!70!black,
        fill=orange!8,
        thick,
        rounded corners=3pt,
        minimum width=3.0cm,
        minimum height=1.05cm,
        align=center
    },
    support/.style={
        draw=blue!55!black,
        fill=blue!5,
        thick,
        rounded corners=3pt,
        minimum width=3.45cm,
        minimum height=1.05cm,
        align=center
    },
    output/.style={
        draw=green!45!black,
        fill=green!10,
        very thick,
        rounded corners=3pt,
        minimum width=3.25cm,
        minimum height=1.10cm,
        align=center
    },
    arr/.style={->, very thick, black!65},
    softarr/.style={->, thick, dashed, black!55},
    bandlabel/.style={font=\footnotesize\bfseries, text=black!65}
]

% Source processing and evidence creation
\node[data] (dataset) {Dataset\\package};
\node[wfstep, right=0.48cm of dataset] (ingest) {Ingestion +\\text extraction};
\node[wfstep, right=0.48cm of ingest] (chunk) {File\\chunking};
\node[wfstep, right=0.48cm of chunk] (overview) {Initial context\\overview};
\node[wfstep, right=0.48cm of overview] (notes) {Evidence note\\extraction};
\node[data, right=0.48cm of notes] (context) {Deduped + aggregated\\evidence context};

\draw[arr] (dataset) -- (ingest);
\draw[arr] (ingest) -- (chunk);
\draw[arr] (chunk) -- (overview);
\draw[arr] (overview) -- (notes);
\draw[arr] (notes) -- (context);

\node[bandlabel, above=0.25cm of overview] {source processing and evidence creation};

% Profile draft construction
\node[support, below=1.15cm of chunk] (profile) {Selected application profile\\{\footnotesize DCAT-AP+ schema}};
\node[wfstep, right=0.55cm of profile] (draftinit) {DCAT-AP draft\\initialization};
\node[wfstep, right=0.55cm of draftinit] (patching) {Semantic requirement\\iteration + patching};
\node[decision, right=0.55cm of patching] (unresolved) {Unresolved requirements\\flagged};
\node[data, right=0.55cm of unresolved] (curated) {Curated or accepted\\profile draft};

\draw[arr] (profile) -- (draftinit);
\draw[arr] (draftinit) -- (patching);
\draw[arr] (patching) -- (unresolved);
\draw[arr] (unresolved) -- node[above, font=\footnotesize, text=black!60] {curate or accept} (curated);
\draw[softarr] (context.south) |- (draftinit.north);
\draw[softarr] (context.south) |- (patching.north);

\node[bandlabel, above=0.25cm of patching] {profile construction};

% Vocabulary grounding
\node[support, below=1.15cm of profile] (mixins) {\texttt{ClassifierMixin}\\profile shapes};
\node[support, right=0.55cm of mixins] (graph) {Property graph with\\vocabulary resources};
\node[wfstep, right=0.55cm of graph] (query) {Vocabulary query\\{\footnotesize Voc4Cat, QUDT, nmrCV}};
\node[wfstep, right=0.55cm of query] (ground) {Ground applicable\\profile values};
\node[output, right=0.55cm of ground] (result) {Grounded metadata\\draft};

\draw[arr] (mixins) -- (graph);
\draw[arr] (graph) -- (query);
\draw[arr] (query) -- (ground);
\draw[arr] (ground) -- (result);
\draw[softarr] (curated.south) |- (ground.north);

\node[bandlabel, above=0.25cm of query] {vocabulary grounding};

% Boundary
\draw[TUgreen!65!black, thick, rounded corners=6pt]
    ($(dataset.north west)+(-0.28,0.55)$) rectangle ($(result.south east)+(0.28,-0.35)$);
\node[fill=white, text=TUgreen!45!black, font=\small\bfseries]
    at ($(dataset.north west)!0.5!(result.north east)+(0,0.55)$) {SIMONE conceptual workflow};

\end{tikzpicture}
