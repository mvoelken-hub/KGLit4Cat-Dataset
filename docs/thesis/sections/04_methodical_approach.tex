\label{chap:requirements}

This chapter derives the requirements and evaluation design for the proposed workflow.

\section{Workflow Architecture}
\label{sec:workflow_architecture}

The workflow developed in this thesis addresses the conversion of heterogeneous catalysis-related research data packages into structured, semantically enriched metadata. Its starting point is the observation that experimental data only becomes reusable when it is accompanied by contextual information about the investigated material, the data-generating activity, the analytical method, relevant parameters, units, files, and provenance. In current catalysis research, this context is often distributed across semi-structured instrument exports, descriptive text files, tabular metadata, images, and nested archive structures. Manual metadata creation can in principle recover this context, but it is time-consuming, inconsistent, and unlikely to scale to routine research-data management. Consequently, the workflow is designed as an automated assistance system that extracts candidate metadata from existing dataset packages and projects it into a controlled metadata profile. This follows the broader FAIR-oriented motivation of catalysis data infrastructures, where structured metadata and shared vocabularies are prerequisites for reproducibility, interoperability, and downstream data-driven analysis \cite{mendes2025data,schumann2025nomad,nfdi4cat2024whitepaper}.

A central design decision is that the extraction is not implemented as a single large prompt. A monolithic prompt would have to contain the task description, target schema, raw file contents, vocabulary descriptions, graph context, and the requested output representation at the same time. This is unsuitable for the present use case because dataset packages may contain multiple files and because semantic grounding requires additional context from controlled vocabularies. Even when a model offers a comparatively large context window, the useful context must be shared between source evidence and external semantic resources. The workflow therefore decomposes the overall task into smaller stages that can be executed, inspected, repeated, and validated independently. Figure~\ref{fig:context-window-bottleneck} illustrates this context-window bottleneck and motivates the staged architecture used in the prototype.

\begin{figure}[H]
    \centering
    \resizebox{\textwidth}{!}{%
        \input{figures/method_context_window}
    }
    \caption{Context-window bottleneck of a single-prompt extraction design. Instructions, profile schema, source files, vocabulary resources, and output constraints compete for the same limited model context. The proposed workflow avoids this by decomposing the task into separate extraction, grounding, and projection stages.}
    \label{fig:context-window-bottleneck}
\end{figure}

The resulting architecture is a staged extraction and enrichment pipeline. The input is a dataset archive, which is first unpacked into individual file entries. Text is extracted from files where possible and retained together with file-level provenance. The extracted files are then ranked according to their expected relevance for metadata extraction. This ranking step is necessary because research data packages often contain a mixture of descriptive metadata files, instrument files, processed data, images, and raw measurements. Context-rich files such as descriptions, manifests, method files, reports, and compact processed tables are therefore processed before files that are unlikely to contain metadata.

After ranking, the textual content is split into semantic chunks. The purpose of chunking is not only to reduce prompt size, but also to preserve local topical coherence. Long technical files frequently contain blocks for different acquisition parameters, processing settings, sample information, and instrument configuration. Treating the entire file as one prompt would obscure this internal structure, whereas fixed-length chunking could cut through meaningful parameter groups. The workflow therefore uses semantic chunking based on embedding distances between neighboring text windows. Low distance indicates that adjacent lines likely belong to the same topical region, while high distance is interpreted as a potential internal boundary.

Each chunk is then processed by a Large Language Model (LLM) to produce an intermediate extraction context. This intermediate representation is intentionally separated from the final metadata profile. It records the extracted data-generating activities, methods, evaluated entities, agents, resources, quantitative attributes, and qualitative attributes before they are forced into a specific application profile. This separation reduces coupling between source interpretation and output formatting. It also makes the workflow more inspectable: errors can be attributed to chunking, extraction, semantic grounding, or profile projection instead of being hidden inside one end-to-end generation step. Similar LLM-based extraction approaches have shown the practical value of prompt-driven extraction from heterogeneous catalysis literature, but the present workflow adapts this idea to dataset-level metadata packages and combines it with vocabulary-backed normalization \cite{walls2025catminer}.

The extracted context is subsequently enriched through controlled-vocabulary lookup. Quantitative attributes are normalized against quantity-kind and unit vocabularies, while qualitative terms such as methods, materials, instrument parts, and domain concepts are matched against chemistry- and catalysis-related vocabularies. This stage does not expose the complete vocabulary graph to the LLM. Instead, a smaller candidate set is retrieved through a hybrid strategy that combines vector-based semantic similarity, lexical full-text search, and local graph expansion. The LLM then selects the most plausible candidate from this constrained context or leaves the field unresolved when no candidate is suitable. This design keeps vocabulary grounding auditable because the candidate set, selected URI, and unresolved cases can be stored as workflow artifacts.

The last stage is profile projection. The workflow takes the merged extraction context, vocabulary normalization results, warnings, and a selected application profile as input and produces a schema-valid metadata document. In the thesis prototype, this profile-oriented architecture is aligned with the idea of DCAT-AP Plus and ChemDCAT-AP-style metadata profiles, where general dataset-level metadata is extended by domain-specific context about scientific data generation \cite{stroemert2026chemdcat}. The application profile provides the document-level target structure, while the vocabularies provide term-level semantic grounding. This distinction is important: vocabularies answer which concept or unit a value refers to, whereas the profile defines where the resulting information belongs in the final metadata document.

Figure~\ref{fig:workflow-architecture} summarizes the complete workflow. The upper row represents source processing and chunk-wise extraction. The middle row represents semantic grounding with controlled vocabularies. The lower row represents profile projection into a final metadata document. Dashed arrows indicate that the same intermediate extraction context is reused by later stages rather than discarded after the first LLM call.

\begin{figure}[H]
    \centering
    \resizebox{\textwidth}{!}{%
        \input{figures/method_workflow_architecture}
    }
    \caption{Overall workflow architecture of the SIMONE prototype. Dataset packages are unpacked, ranked, chunked, and processed by chunk-wise extraction. The resulting intermediate extraction context is semantically grounded against controlled vocabularies and finally projected into a selected application profile.}
    \label{fig:workflow-architecture}
\end{figure}

The workflow boundary is deliberately limited to metadata extraction and semantic enrichment. It does not claim to fully interpret every scientific file format contained in a dataset package, nor does it replace domain-expert validation. Binary files, rendered images, spectra, and other non-textual resources may be retained as dataset resources even when their internal content is not directly interpreted. The primary output of the workflow is therefore a structured metadata proposal with traceable source evidence and vocabulary mappings. This proposal can be inspected, corrected, and evaluated against a manually curated reference in the evaluation chapter.

From an architectural perspective, the workflow separates four concerns. First, source handling converts a heterogeneous archive into ordered, text-accessible evidence units. Second, extraction converts local evidence into a domain-neutral intermediate representation. Third, semantic grounding connects extracted values to controlled vocabularies and records the matching process. Fourth, profile projection converts the normalized extraction context into a target metadata schema. This separation supports modular evaluation: object extraction, value extraction, URI mapping, schema validity, and traceability can be assessed independently. It also leaves room for future extensions, such as replacing the chunking strategy, adding file-format-specific parsers, changing the LLM backend, or introducing additional application profiles without redesigning the complete workflow.
